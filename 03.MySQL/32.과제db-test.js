const dm = require('./31.과제db-module');

/* dm.getGirlGroupList(rows => {
    for (let row of rows) {
        console.log(row.gid, row.name, row.debutDate, row.title);
    }
}); */

/* dm.getSongList(rows => {
    for (let row of rows) {
        console.log(row.sid, row.title, row.lyrics, row.name);
    }
}); */

/* dm.getGirlGroup(2, rows => {
    for (let row of rows) {
        console.log(row.gid, row.name, row.debutDate, row.title);
    }
}); */

/* dm.getSong(102, rows => {
    for (let row of rows) {
        console.log(row.sid, row.title, row.lyrics, row.name);
    }
}); */

/* dm.insertGirlGroup(['EXID','2012-07-30',116], () => {
    dm.getGirlGroupList(rows => {
        for (let row of rows) {
            console.log(row.gid, row.name, row.debutDate, row.title);
        }
    });
}); */

/* dm.updateGirlGroup(['EXID','2012-08-30',115, 13], () => {
    dm.getGirlGroup(13, rows => {
        for (let row of rows) {
            console.log(row.gid, row.name, row.debutDate, row.title);
        }
    });
}); */

/* dm.deleteGirlGroup(14, () => {
    dm.getGirlGroupList(rows => {
        for (let row of rows) {
            console.log(row.gid, row.name, row.debutDate, row.title);
        }
    });
}); */

